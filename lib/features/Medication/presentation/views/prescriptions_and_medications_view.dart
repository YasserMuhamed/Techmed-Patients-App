import 'package:flutter/material.dart';

class PrescriptionsAndMedicationsView extends StatelessWidget {
  const PrescriptionsAndMedicationsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("Prescriptions and Medications View")),
    );
  }
}
